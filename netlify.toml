[build]
  base = "."
  command = "npm install --prefix portfolio && npm run build --prefix portfolio"
  publish = "portfolio/build"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "REACT_APP_SUPABASE_URL,REACT_APP_SUPABASE_ANON_KEY"

